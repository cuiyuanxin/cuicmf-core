/**
  项目后台JS主入口
  以依赖layui
**/    
layui.define(['layer', 'element', 'form'], function(exports){
    var layer = layui.layer
        ,element = layui.element
        ,form = layui.form;
    layer.msg(123);
    // 监控清除缓存按钮
    form.on('submit(cui_clear)', function(data){
        console.log(data);
//        ajax({
//            url: $('#cuiFrom').attr('action'),
//            data: data.field
//        });
        return false;
    });
  
    exports('common', {});
}); 